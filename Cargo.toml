[package]
name = "cryptshield-rust"
version = "0.1.0"
edition = "2021"
description = "Rust and Slint native CryptShield desktop application"
authors = ["fuadfaut"]
build = "build.rs"

[dependencies]
slint = "1.16.0"
toml = "0.9.12"
toml_edit = "0.25.11"

[build-dependencies]
slint-build = "1.16.0"
